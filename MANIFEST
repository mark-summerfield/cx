# TODO
# (0) If can't get tag_cmp() working, try removing cmp & ∴ vec_find,
#     vec_sort, vec_search, & show how these can be done using external
#     functions in vec_test.[hc].
# (1) vec tests: copy vec_str_test.[hc] & test on Tag type:
#	alloc free get get_last set insert replace remove pop copy merge
#	equal find sort search
#
# (2) set types
#	- set void* (e.g., struct KeyValue, in effect a map)
#	- set_int
#	- set_str
#     based on some kind of b?tree (or fall back to rbt?)
#
# (3) hsh with void* items - as with set but based on hash table
# (4) que (based on vec)?
# (5) lst (doubly-linked list)?
# (6) deq (based on list)?
#
# (7) str (based on vec)?

cx.h
vec_common.h
vec.h
vec.c
vec_str.h
vec_str.c
vec_int.h
vec_int.c

sx.h
sx.c

vaargs.h

cx_test.c
cx_util_test.h
cx_util_test.c
vec_common_test.h
vec_common_test.c
vec_int_test.h
vec_int_test.c
vec_str_test.h
vec_str_test.c
tag_test.h
vec_test.h
vec_test.c # TODO
sx_test.h
sx_test.c
va_test.h
va_test.c

SConstruct

doc.adoc
doc.rb
doc.css
doc.sh

st.sh

# vim: syn=yaml
